name = "ksp-gives-back"
main = "src/index.js"
compatibility_date = "2024-12-01"

# ── Static Assets ──
[assets]
directory = "./public"

# D1 and KV bindings are configured in the Cloudflare dashboard
# under Worker Settings → Bindings (not in this file for deployment)
